-- +goose Up
-- Initial migration placeholder. Replace with the first real schema change.
-- Reminder: use VARCHAR for enumerated fields, validated in app code — no DB enums.
SELECT 1;

-- +goose Down
SELECT 1;
